{
  "name": "math-lumina-learning-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless Math Lumina Learning Engine domain kernel — Phase 2 Slice 1.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/**/*.test.ts",
    "check": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
